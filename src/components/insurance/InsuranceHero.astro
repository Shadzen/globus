---
import HeroNav from '@/components/HeroNav.astro'

import heroDesktop from '@/assets/images/insurance/hero-1.jpg'
import heroMobile from '@/assets/images/insurance/hero-1-mobile.jpg'

import { BREAKPOINTS } from '@/scripts/utils'

interface Props {
    userType?: 'tourist' | 'agent'
}

const { userType = 'tourist' } = Astro.props
const base = import.meta.env.BASE_URL
---

<section class="hero-section insurance-hero _dark-text _type-two">
    <picture class="hero-bg">
        <source media={`(max-width: ${BREAKPOINTS.mobile}px)`} srcset={heroMobile.src} />
        <img src={heroDesktop.src} alt="" loading="eager" decoding="async" />
    </picture>

    <HeroNav items={[
        { label: 'Главная', href: `${base}${userType}/` },
        { label: 'Страхование' },
    ]} />

    <div class="hero-content">
        <h2 class="hero-title">Страхование</h2>
        <p class="hero-subtitle">Отправляясь в отпуск в другой город или страну, все надеются на яркие впечатления, интересные встречи и положительные эмоции. Минимизировать риски, с которыми связаны дальние поездки, позволяет туристическое страхование.</p>
        <p class="hero-subtitle">Страховка для туристов помогает обезопасить себя от траты времени, нервов и денег в другом городе или чужой стране в случае получения травмы или болезни, потери багажа или документов, а также в других ситуациях, когда вам будет необходима помощь. Туристическая страховка даст вам возможность чувствовать себя уверенно в любом путешествии.</p>
    </div>
</section>

<style lang="scss" is:global>

</style>
