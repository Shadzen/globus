---
const base = import.meta.env.BASE_URL
---

<section class="partner-selfie-section">
    <div class="partner-selfie-container">
        <h2>Сотрудничество с Selfie Travel</h2>

        <div class="partner-selfie-content">
            <p class="partner-selfie-text">
                Для начала работы с турами Selfie Travel просим следовать инструкции
            </p>

            <a href={`${base}agent/selfie_travel`} class="partner-selfie-button">
                <span>Selfie Travel</span>
            </a>
        </div>
    </div>
</section>

<style lang="scss" is:global>
    .partner-selfie-section {
        @include adaptive(margin-top, 80, 60);

        .partner-selfie-container {
            @include side-paddings;
            width: 100%;
        }

        .partner-selfie-content {
            @include adaptive(margin-top, 34, 30);
            @include adaptive(padding-left, 145, 0);
            @include adaptive(padding-right, 145, 0);
            // margin-left: auto;
            // margin-right: auto;
            // width: 80%;
            @include flex-center;
            @include adaptive(gap, 60, 60);
            flex-wrap: wrap;
            align-items: center;

            @include mobile {
                flex-direction: column;
            }
        }

        .partner-selfie-text {
            @include font-circe-light;
            @include adaptive(font-size, 26, 18);
            color: $color-alternate;
            line-height: 1.2;
            flex: 1;

            @include mobile {
                text-align: center;
            }
        }

        .partner-selfie-button {
            @include button-sized;
        }
    }
</style>
