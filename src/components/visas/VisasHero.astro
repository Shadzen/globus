---
import HeroNav from '@/components/HeroNav.astro'

import heroDesktop from '@/assets/images/visas/hero-1.jpg'
import heroMobile from '@/assets/images/visas/hero-1-mobile.jpg'

interface Props {
    userType?: 'tourist' | 'agent'
}

const { userType = 'tourist' } = Astro.props
const base = import.meta.env.BASE_URL
---

<section class="hero-section visas-hero">
    <picture class="hero-bg">
        <source media="(max-width: 480px)" srcset={heroMobile.src} />
        <img src={heroDesktop.src} alt="" loading="eager" decoding="async" />
    </picture>

    <HeroNav items={[
        { label: 'Главная', href: `${base}${userType}/` },
        { label: 'Визы' },
    ]} textColor="dark" />

    <div class="hero-content">
        <h2 class="hero-title">Визы</h2>
        <p class="hero-subtitle">Собственный визовый центр «Глобус-тур» — это гарантия надёжности и уверенности в результате.</p>
        <p class="hero-subtitle">Вы можете рассчитывать на индивидуальную консультацию, сопровождение на всех этапах получения визы и высокую скорость обработки заявок.</p>
    </div>
</section>

<style lang="scss" is:global>
    .visas-hero {
        @include adaptive(height, 940, 704);
        @include adaptive(padding-top, 440, 518);

        .hero-bg {
            @include adaptive(height, 360, 231);
            @include adaptive(width, 1720, 450);
            bottom: 0;
            @include adaptive(left, 100, 15);

            @include desktop {
                top: auto;
            }

            @include mobile {
                @include adaptive(top, 250, 250);
            }
        }

        .hero-content {
            z-index: 2;
            flex-wrap: wrap;
            flex-direction: row;
            flex: 0;
            @include adaptive(column-gap, 20, 20);
            justify-content: space-between;
        }

        .hero-title {
            color: $color-alternate;
            text-align: left;

            @include desktop {
                width: 22%;
            }
        }

        .hero-subtitle {
            color: $color-alternate;
            @include adaptive(padding-top, 2, 16);

            @include desktop {
                &:nth-child(2) {
                    width: 31%;
                }
                &:nth-child(3) {
                    width: 41%;
                }
            }
        }
    }
</style>
