---
// src/components/main/ContactFormPopupContent.astro
// Popup variant of contact form (tourist structure, light/white background).
import SocialButtons from '@/components/common/SocialButtons.astro'
---

<div class="contact-form-popup">
    <h2 id="contact-title" data-modal-focus>Открываем мир</h2>

    <div class="contact-form-container">
        <div class="contact-form-left">
            <div class="contacts-info">
                <h3>Контакты для связи</h3>

                <div class="contacts-info-item">
                    <span class="contacts-info-label">Экстренный телефон</span>
                    <div class="contacts-info-separator"></div>
                    <a href="tel:+7 383 2000 500" class="contacts-info-value"> +7 383 2000 500 </a>
                </div>

                <div class="contacts-info-item">
                    <span class="contacts-info-label">Телефон</span>
                    <div class="contacts-info-separator"></div>
                    <a href="tel:+7 383 2000 500" class="contacts-info-value"> +7 383 2000 500 </a>
                </div>

                <div class="contacts-info-item">
                    <span class="contacts-info-label">Email</span>
                    <div class="contacts-info-separator"></div>
                    <a href="mailto:info@globus-tour.ru" class="contacts-info-value">
                        info@globus-tour.ru
                    </a>
                </div>
            </div>

            <div class="contacts-social">
                <h3>Быстрая связь в месенджерах</h3>
                <SocialButtons
                    variant="default"
                    telegramHref="https://t.me/+79130637716"
                    whatsappHref="https://wa.me/79130637716"
                />
            </div>
        </div>

        <div class="contact-form-right">
            <form class="contact-form">
                <h3>Напишите прямо сейчас</h3>

                <input
                    type="text"
                    id="contact-name"
                    name="name"
                    class="contact-form-input"
                    placeholder="Имя"
                    aria-label="Имя"
                    autocomplete="name"
                />
                <input
                    type="tel"
                    id="contact-phone"
                    name="phone"
                    class="contact-form-input"
                    placeholder="Телефон"
                    aria-label="Телефон"
                    autocomplete="tel"
                />
                <input
                    type="text"
                    id="contact-comment"
                    name="comment"
                    class="contact-form-input"
                    placeholder="Комментарий"
                    aria-label="Комментарий"
                    autocomplete="off"
                />

                <div class="contact-form-checkbox">
                    <input type="checkbox" id="contact-agree" name="agree" required />
                    <label for="contact-agree">
                        Я принимаю условия <a href="#">Пользовательского соглашения</a> и подтверждаю
                        <a href="#">согласие на обработку моих персональных данных</a>.
                    </label>
                </div>

                <button type="submit" class="button _sized">Отправить</button>
            </form>
        </div>
    </div>
</div>

<style lang="scss" is:global>
    .contact-form-popup {
        display: flex;
        flex-direction: column;
        @include adaptive(gap, 80, 40);

        .contact-form-container {
            display: flex;
            @include adaptive(gap, 100, 60);

            @include mobile {
                flex-direction: column;
            }
        }

        h3 {
            @include adaptive(font-size, 28, 28);
            color: var(--color-globus-blue);

            @include mobile {
                text-align: center;
            }
        }

        .contact-form-left {
            flex: 1;
            display: flex;
            flex-direction: column;
            @include adaptive(gap, 32, 60);

            @include mobile {
                order: 2;
            }
        }

        .contacts-info-item {
            @include adaptive(gap, 24, 14);
            width: 100%;
        }

        .contact-form-right {
            flex: 1;
            display: flex;
            align-items: flex-start;

            @include mobile {
                order: 1;
            }
        }

        .button._sized {
            @include adaptive(width, 222, 100%);
            align-self: flex-start;
        }
    }
</style>
